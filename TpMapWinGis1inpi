Yes — 100% this is a Lombok problem, not a MapStruct problem and not a Java problem.

Your errors all say:

cannot find symbol: getDocuments()
cannot find symbol: getAlerts()
cannot find symbol: getRepresentatives()
cannot find symbol: getSecondaryOffices()
cannot find symbol: getBeneficialOwners()

➜ These are all Lombok-generated getters, which means your IDE is not generating Lombok methods.

⸻

✅ Why this happens

Your pom.xml is correct — Lombok is included:

<dependency>
    <groupId>org.projectlombok</groupId>
    <artifactId>lombok</artifactId>
    <optional>true</optional>
</dependency>

But IntelliJ finally needs a plugin + annotation processing enabled.

When Lombok is not processed, your model classes contain no getters, so MapStruct and your own code cannot find them → compiler errors.

⸻

✅ Fix Lombok in IntelliJ (VERY IMPORTANT)

✔️ Step 1: Enable Annotation Processing

Go to:

File → Settings → Build, Execution, Deployment → Compiler → Annotation Processors

Enable:

✔️ “Enable annotation processing”
✔️ “Obtain processors from classpath”

⸻

✔️ Step 2: Install Lombok Plugin

Go to:

File → Settings → Plugins → Marketplace

Search → “Lombok”

Install + Restart IDE

⸻

✔️ Step 3: Rebuild your project

mvn clean install

or inside IntelliJ:

Build → Rebuild Project


⸻

🔥 After fixing Lombok, all getters/setters will appear

Your classes like:

@Data
public class LegalEntity { ... }

will correctly generate:
	•	getDocuments()
	•	getAlerts()
	•	getRepresentatives()
	•	getBeneficialOwners()
	•	getSecondaryOffices()
	•	setLegalEntityParent()
	•	setParentCompany()

And the errors will disappear.

⸻

If you want, send me your LegalEntity class

I can confirm exactly which Lombok annotations you need.