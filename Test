Voici un message clair et professionnel que tu peux envoyer directement à XRX pour expliquer la situation :

⸻

✅ Message pour XRX

Oui exactement, c’est bien l’idée :
on veut tester le nouveau TS, et donc c’est lui qui doit exécuter les jobs.

Si on laisse l’ancien TS + le nouveau TS lancer les mêmes jobs en parallèle, ça provoque :
	•	🔥 une charge CPU énorme (2 médiators qui traitent les mêmes flux au même moment)
	•	🔁 des duplications de données en environnement dev
	•	📄 double génération de fichiers (PACS, OK files, PayZen…)
	•	🚨 risques d’incohérences dans les intégrations PACS
	•	🧵 2 schedulers qui se battent pour les mêmes locks

Donc oui :
➡️ on doit désactiver les jobs sur l’ancien TS en mettant poolSize=0 dans tournesol-mediator-facade.xml.
➡️ L’ancien TS reste opérationnel pour tout le reste (fichiers, répertoires, services), mais ne lance plus aucun job.

Comme ça, seul le nouveau TS exécute les jobs, et on peut valider son comportement sans interférences.

⸻

Si tu veux, je peux aussi rédiger une version plus courte, plus technique, ou plus interne pour Slack.