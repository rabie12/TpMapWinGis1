
    @Bean
    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
              http
                .csrf(AbstractHttpConfigurer::disable)
                .authorizeHttpRequests(auth -> auth
                        .requestMatchers("/swagger-ui/**", "/v3/api-docs/**").permitAll()
                        .requestMatchers("/api/**").hasAuthority("OLKY_ADMIN")
                        .anyRequest().permitAll()
                )
                .httpBasic(basic -> {})
                .formLogin(AbstractHttpConfigurer::disable)
                .sessionManagement(session -> session.sessionCreationPolicy(SessionCreationPolicy.STATELESS)); // Stateless session

        return http.build();
    }

this root are not working : {{host}}/validate/SPKHDE2H/DE43250501800000198080
but this one are working : {{host}}/deriveBICFromIBAN/LU260025410089543900
value of host : http://localhost:9005/api
can you tell me why 
