       if (existingRecord.isPresent()) {
            log.info("BIC found in database.");
            Optional<BankInfoDTO> bankInfoDTO = existingRecord.map(bankInfoMapper::toDto);
            bankInfoDTO.get().setStatus("VALID");
            List<CountryCode> codes = CountryCode.findByName("(?i)^" + bankInfoDTO.get().getLocation() + "$");
            CountryCode code = codes.get(0);
            bankInfoDTO.get().setCountryCode(String.valueOf(code.getNumeric()));
            bankInfoDTO.get().setName(bankInfoDTO.get().getInstitution());
            bankInfoDTO.get().setCountryName(code.getName());
            return bankInfoDTO;
        }

            CountryCode code = codes.get(0);
check ici si c'est vide et si tu peux ajouter une recuperation via la ville en utilisant la import com.neovisionaries.i18n sinon enlève la recherche
