Parfait 👌 tu utilises Bruno, donc voici exactement comment faire dans cet outil (c’est très proche de Postman, mais avec une interface plus légère).

⸻

⚙️ Étapes pour corriger l’erreur dans Bruno

1️⃣ Méthode et URL
	•	Méthode : POST
	•	URL :

http://<ton-serveur>:<port>/api/validate/DE43250501800000198080

(exemple : http://localhost:8080/api/validate/DE43250501800000198080)

⸻

2️⃣ Authentification Basic

Dans Bruno :
	•	Clique sur Auth dans le panneau de gauche ou l’onglet correspondant à ta requête.
	•	Sélectionne Basic Auth
	•	Renseigne :
	•	Username → OLKY_ADMIN
	•	Password → ton mot de passe
	•	(Bruno ajoutera automatiquement l’en-tête Authorization: Basic ...)

⸻

3️⃣ En-tête HTTP

Va dans l’onglet Headers et ajoute cette ligne (si elle n’est pas déjà là) :

Content-Type: application/json


⸻

4️⃣ Corps de la requête
	•	Clique sur Body
	•	Choisis Raw → JSON
	•	Mets un contenu vide ou minimal :

{}

(Si ton service validateIban n’a pas besoin d’infos particulières dans IbanInfoUtilsDTO)

⸻

5️⃣ Envoie la requête 🚀

Clique sur Send.
Tu devrais maintenant obtenir une réponse JSON (et non plus l’erreur Content-Type 'application/x-www-form-urlencoded' is not supported).

⸻

🧩 Pourquoi ça marche

Avant, Bruno envoyait ta requête en form-urlencoded, c’est-à-dire comme un formulaire HTML :

Content-Type: application/x-www-form-urlencoded

Ton contrôleur Spring n’accepte pas ce format, il attend du JSON.
En choisissant Raw JSON + Content-Type: application/json, tu règles le problème.

⸻

Souhaites-tu que je t’indique aussi comment passer un vrai objet IbanInfoUtilsDTO dans le body si ton service en a besoin (par exemple avec des champs comme country, bankCode, etc.) ?