En résumé :

Malgré toutes les tentatives (mise à jour Spring Boot 4, SpringDoc 3.0-M1, springdoc.model-converters.nullable-properties=true, annotation sur champ ou getter, example="null"), SpringDoc ne gère pas correctement example="null" pour générer un vrai null dans OpenAPI 3.1.
nullable=true est pris en compte pour le type (type: ["string","null"]), mais l’exemple reste "null" en string. Il n’y a actuellement aucune solution native simple pour obtenir "example": null sans passer par un customizer.

Bref : l’annotation seule ne suffit pas, c’est une limitation connue de SpringDoc 3.0-M1.