Ah OK — you mean setx as in Windows environment variables, not Liquibase “sets”.
So your question is:

👉 “Is it possible to configure the Liquibase baseline using Windows environment variables with setx?”

✔️ Yes — you can configure all Liquibase parameters (including baseline commands) using Windows environment variables set via setx.

Let me show you exactly how.

⸻

✅ 1. Setting Liquibase environment variables using setx

You can set any Liquibase property like this:

setx LIQUIBASE_URL "jdbc:postgresql://localhost:5432/bankinfo"
setx LIQUIBASE_USERNAME "postgres"
setx LIQUIBASE_PASSWORD "postgres"
setx LIQUIBASE_CHANGELOG_FILE "db.changelog-master.yaml"
setx LIQUIBASE_SEARCH_PATH "C:\RHABACHI\IdeaProjects\bank-info\liquibase\changelog"

After that, restart PowerShell or CMD, then simply run:

.\liquibase.bat update

Liquibase automatically reads environment variables prefixed with LIQUIBASE_.

⸻

✅ 2. But what about baseline?

Liquibase has no “baseline” command, but the equivalent is:

✔ changelogSync

Mark all existing changesets as executed.

✔ And yes — you can also run changelogSync using environment variables defined by setx.

Example:

.\liquibase.bat changelogSync

And it uses the env vars you set via setx.

⸻

✅ 3. Example: full baseline setup using setx

Step 1 — Configure Liquibase environment variables

setx LIQUIBASE_URL "jdbc:postgresql://localhost:5432/bankinfo"
setx LIQUIBASE_USERNAME "postgres"
setx LIQUIBASE_PASSWORD "postgres"
setx LIQUIBASE_CHANGELOG_FILE "db.changelog-master.yaml"
setx LIQUIBASE_SEARCH_PATH "C:\RHABACHI\IdeaProjects\bank-info\liquibase\changelog"

Step 2 — Open a new terminal

(Windows requires restart to load new env vars)

Step 3 — Run baseline-equivalent

cd C:\Users\RHABACHI\Downloads\liquibase-4.33.0\
.\liquibase.bat changelogSync

Step 4 — After baseline, normal updates:

.\liquibase.bat update


⸻

🌟 Summary

Goal	Liquibase command	Can use setx?
Set DB connection	env vars	✔️ Yes
Set changelog/paths	env vars	✔️ Yes
Baseline (mark existing changes as executed)	changelogSync	✔️ Yes
Normal migrations	update	✔️ Yes


⸻

If you want, I can generate a ready-to-copy .bat script that sets all env variables and runs Liquibase automatically.