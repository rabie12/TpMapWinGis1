Got it — you want to:
	1.	Understand what the Liquibase job in your pipeline does, and
	2.	Change your project so that the liquibase folder is OUTSIDE src, so that:
	•	The .csv files are NOT embedded inside the Spring Boot JAR
	•	Liquibase can still run locally and on the dev server

I’ll explain both clearly and give you the exact changes to make.

⸻

✅ 1. What Your Liquibase Pipeline Job Currently Does

This part of your pipeline:

updateLiquibase:
  stage: liquibase
  before_script: []
  environment: $GITLAB_ENV
  variables:
    JAVA_HOME: "/usr/lib/jvm/jdk-21.0.8-oracle-x64/"
    LIQUIBASE_COMMAND_CHANGELOG_FILE: "db.changelog-master.yml"
    LIQUIBASE_SEARCH_PATH: "./src/main/resources/liquibase"
  script:
    - liquibase update
  when: manual
  tags:
    - "$GITLAB_TAG"

🔎 Meaning:

Line	Meaning
when: manual	You must click the button manually in GitLab to run Liquibase
LIQUIBASE_SEARCH_PATH	Liquibase looks for files in: src/main/resources/liquibase
LIQUIBASE_COMMAND_CHANGELOG_FILE	Main changelog file
liquibase update	Applies DB migrations

✅ Right now, your CSV files are inside:

src/main/resources/liquibase

So Spring Boot packages them inside the JAR, which is what your team wants to avoid.

⸻

✅ 2. What Your Team Is Asking You To Do

“le dossier liquibase doit etre sorti de src afin de pas retrouver les fichiers .csv dans le jar”

This means:

✅ Liquibase files must be outside src/
✅ CSV files must be external files, NOT inside the JAR
✅ The JAR stays clean
✅ Liquibase still works in:
	•	Local
	•	Dev server
	•	GitLab pipeline

⸻

✅ 3. New Correct Project Structure (Target)

You should change from this:

src/main/resources/liquibase/

✅ To this:

liquibase/
  ├── db.changelog-master.yml
  ├── changes/
  ├── data/
  │   └── data.csv

✅ Your project root should look like this:

bank-info/
├── liquibase/
│   ├── db.changelog-master.yml
│   ├── changes/
│   └── data/
├── src/
├── pom.xml
├── target/

🚨 This guarantees that CSV files are NOT inside the JAR anymore.

⸻

✅ 4. Update Your GitLab Liquibase Job (VERY IMPORTANT)

Change this line in your pipeline:

❌ Current:

LIQUIBASE_SEARCH_PATH: "./src/main/resources/liquibase"

✅ Replace with:

LIQUIBASE_SEARCH_PATH: "./liquibase"

So your job becomes:

updateLiquibase:
  stage: liquibase
  before_script: []
  environment: $GITLAB_ENV
  variables:
    JAVA_HOME: "/usr/lib/jvm/jdk-21.0.8-oracle-x64/"
    LIQUIBASE_COMMAND_CHANGELOG_FILE: "db.changelog-master.yml"
    LIQUIBASE_SEARCH_PATH: "./liquibase"
  script:
    - liquibase update
  when: manual
  tags:
    - "$GITLAB_TAG"

✅ Now GitLab will correctly read your external Liquibase folder.

⸻

✅ 5. How To Manage This on the DEV Server

You showed this on the server:

bank-info.env  
bank-info.jar  
java.env  
logs  
startup.sh

You must now ADD liquibase next to the JAR:

✅ Final structure on the server:

/opt/bank-info/
├── bank-info.jar
├── liquibase/
│   ├── db.changelog-master.yml
│   ├── changes/
│   └── data/
│       └── data.csv
├── bank-info.env
├── java.env
├── logs
├── startup.sh


⸻

✅ 6. How To Run Liquibase on the DEV Server

From the project root on the server:

liquibase \
  --search-path=./liquibase \
  --changeLogFile=db.changelog-master.yml \
  update

✅ This applies DB changes using external CSV files
✅ No CSVs inside the JAR
✅ Matches your new GitLab behavior

⸻

✅ 7. IMPORTANT: Spring Boot Must NOT Reference Liquibase Anymore

If you currently have in application.yml or application.properties:

spring:
  liquibase:
    enabled: true

⚠️ This may try to run Liquibase from inside the JAR
Since Liquibase is now external, you should:

✅ Either disable Spring Liquibase:

spring:
  liquibase:
    enabled: false

✅ And rely ONLY on:
	•	GitLab job
	•	Manual server Liquibase runs

⸻

✅ 8. Summary (What You Must Do)

Task	Status
Move liquibase/ out of src/	✅ Required
Update LIQUIBASE_SEARCH_PATH in GitLab	✅ Required
Copy liquibase/ next to JAR on dev server	✅ Required
Run Liquibase from external folder	✅ Required
Disable Spring Boot Liquibase (if enabled)	⚠️ Recommended


⸻

✅ If You Want, I Can Also:
	•	✅ Review your db.changelog-master.yml
	•	✅ Check if your CSV loadData paths will still work
	•	✅ Adapt your startup.sh if Liquibase must auto-run at startup

Perfect — please paste the following files here (you can anonymize sensitive values):
	1.	✅ liquibase/db.changelog-master.yml
	2.	✅ One example of a changelog file that uses a CSV (the file inside changes/)
	3.	✅ (Optional but recommended) your:
	•	application.yml or application.properties
	•	startup.sh from the server

⸻

What I will do for you once you paste them:

✅ Fix all file paths for the new external ./liquibase structure
✅ Ensure all loadData CSV paths work correctly
✅ Prevent Liquibase from trying to run inside the JAR
✅ Make sure:
	•	GitLab ✅
	•	Local ✅
	•	Dev server ✅
All behave exactly the same

⸻

👉 Go ahead and paste your db.changelog-master.yml first.