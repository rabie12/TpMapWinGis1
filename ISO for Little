OK — I can clearly see the root cause of your error now:

changelogMaster.yaml does not exist

This means Liquibase cannot find the file you reference.
And after checking your command, the real problem is the path and the filename.

Let me fix everything for you step by step.

⸻

❌ YOUR CURRENT COMMAND (not correct)

.\liquibase.bat ^
 --search-path="C:\RHABACHI\IdeaProjects\bankinfo\liquibase\" ^
 --changelog-file=changelogMaster.yaml ^
 --url="jdbc:mariadb://localhost:3306/external_bank_data" ^
 --username=root ^
 --password=root ^
 update

🔥 PROBLEMS:
	1.	Wrong filename
You use:

changelogMaster.yaml

But in all your previous messages, your file was named:

db.changelog-master.yaml

OR inside changelog folder.

	2.	Your search-path folder is wrong
You set:

...\bankinfo\liquibase\

But earlier you told me your structure was:

bankinfo\liquibase\changelog\db.changelog-master.yaml

So your search-path must point to “changelog”, not the parent.

	3.	Your environment variables are NOT set
When you run:

echo LIQUIBASE_SEARCH_PATH

You get just the literal name — that means the variable is NOT set.
To check a variable in PowerShell you must use $env: :

echo $env:LIQUIBASE_SEARCH_PATH



⸻

✅ THE FIX

✔ Step 1 — Confirm your folders

Please check this folder exists:

C:\RHABACHI\IdeaProjects\bankinfo\liquibase\changelog\db.changelog-master.yaml

If yes → proceed.

⸻

✔ Step 2 — Use the correct PowerShell command

Here is the corrected command:

.\liquibase.bat ^
  --search-path="C:\RHABACHI\IdeaProjects\bankinfo\liquibase\changelog" ^
  --changelog-file=db.changelog-master.yaml ^
  --url="jdbc:mariadb://localhost:3306/external_bank_data" ^
  --username=root ^
  --password=root ^
  update

💡 Notice:
	•	I removed the trailing backslash "....\liquibase\"
	•	I added the missing changelog folder
	•	I fixed the filename to db.changelog-master.yaml

⸻

✔ Step 3 — If you want to check your file exists

Run:

ls "C:\RHABACHI\IdeaProjects\bankinfo\liquibase\changelog"

You should see:

db.changelog-master.yaml

If it’s not there, Liquibase will never find it.

⸻

✔ Step 4 — Check environment variables in PowerShell correctly

You MUST check with:

echo $env:LIQUIBASE_SEARCH_PATH
echo $env:LIQUIBASE_COMMAND_CHANGELOG_FILE

NOT with:

echo LIQUIBASE_SEARCH_PATH

That prints only the literal text.

⸻

✅ If you want I can detect the exact error in 10 seconds

Please run this and paste the output:

tree C:\RHABACHI\IdeaProjects\bankinfo\liquibase /F

I will immediately tell you what is wrong with the path or filename.