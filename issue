Great — this confirms the situation 100%:

👉 This POM is NOT the root parent.
It has:

<parent>
    <groupId>eu.olkypay</groupId>
    <artifactId>olkypay</artifactId>
    <version>9-SNAPSHOT</version>
</parent>

That means:

❗ The Java 17 configuration is inside:

eu.olkypay : olkypay : 9-SNAPSHOT

This POM you showed does NOT contain Java 17,
meaning the Java version is inherited from the parent, not defined here.

Your properties:

<maven.compiler.source>1.8</maven.compiler.source>
<maven.compiler.target>1.8</maven.compiler.target>

are fine — but they are being OVERRIDDEN by the real parent.

⸻

🔥 So THIS is your problem:

The real parent:

eu.olkypay/olkypay/9-SNAPSHOT/pom.xml

contains either:
	•	<maven.compiler.release>17>
	•	or a plugin definition like:

<maven-compiler-plugin>
    <configuration>
        <release>17</release>
    </configuration>
</maven-compiler-plugin>

or it imports a BOM that sets Java 17.

⸻

✅ FIX

You MUST edit the top-level parent POM:

Open:

olkypay/pom.xml

Search for anything like:

17
<release>
<source>
<target>
<maven.compiler.*

And change it to Java 8:

<maven.compiler.source>1.8</maven.compiler.source>
<maven.compiler.target>1.8</maven.compiler.target>
<maven.compiler.release>8</maven.compiler.release>

Or remove <release> completely.

⸻

✔ Why your modules still compile with Java 17

Your modules inherit in this order:

olkypay  (root)   ← sets Java 17
   ↓
olkypay-core      ← inherits Java 17
   ↓
olkypay-core-util ← inherits Java 17

So even though you put Java 8 here, parent wins because Maven property inheritance works like:
	•	If parent defines property → child inherits it
	•	If child redefines property → child overrides it
	•	But your parent probably sets maven-compiler-plugin directly, not just properties → plugin ALWAYS overrides child properties

Which means your Java 8 property is ignored.

⸻

👉 Next step (simple):

Please paste the root parent POM:

eu.olkypay/olkypay/9-SNAPSHOT/pom.xml

It is the one causing the Java 17 target. I will tell you exactly which lines to change.